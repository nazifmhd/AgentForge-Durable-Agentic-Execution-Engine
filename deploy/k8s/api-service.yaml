apiVersion: v1
kind: Service
metadata:
  name: agentforge-api
  labels: { app.kubernetes.io/name: agentforge, app.kubernetes.io/component: api }
spec:
  selector: { app.kubernetes.io/name: agentforge, app.kubernetes.io/component: api }
  ports:
    - { name: http, port: 80, targetPort: http }
